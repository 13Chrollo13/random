#include "config.h"
void setup() {
  Serial.begin(115200);
  bool fertig = false 
  delay(2000);

  Serial.println("guess a number between 0 and 10");
}



void loop() {
  while (bool fertig == false) {
    Serial.println("1")
  }
    //runden_anzahl += 1;
    //if (Serial.available()) {
      //user_input = Serial.readStringUntil('\n');
      //if (user_input == zahl) {
        //Serial.println("das ist richtig");
       // fertig = true;
      }
      //if (user_input != zahl) {
        //Serial.println("das ist falsch dau hast noch " + "versuche");
      }
    
    //Serial.println("Nice to meet you, " + my_name + "!");
  }